import styled from 'styled-components/native';
import themes from '../../themes/themes';

export const Container = styled.View`
  width: 100%;
  height: 45px;
  background-color: ${themes.theme.silver};
  justify-content: space-between;
`;
